# Rejected AWS signer proposals

These files preserve the historical Fargate/KMS/DynamoDB proposal for audit
traceability only. The project explicitly does not use AWS. Nothing in this
directory is an approved architecture, deployment input, operator procedure,
or production fallback.

The active non-AWS design is
[`../../VPS_SIGNER_ARCHITECTURE.md`](../../VPS_SIGNER_ARCHITECTURE.md), and the
runtime rejects legacy AWS signer configuration.
