# LITHO L1 R1.1 M03 independent-observer attestation

- Independent observer: `REQUIRED — must not be BrewCodeDev`
- Durable GitHub identity: `REQUIRED`
- Execution operator: BrewCodeDev (`@BrewCodeDev`)
- Evidence payload SHA-256:
  `400beb8d088861a37a3f79615f4d32fab19cd77d4ba358943a48ee89d1ea7aa0`
- Verification timestamp (UTC): `REQUIRED`

I independently verified the candidate binary, Makalu chain identity, raw
transaction and receipt hashes, control transaction, host-health evidence,
manifests, detached organizational signature, and disclosed limitations in the
identified payload. I confirm that I did not execute the regression
transactions and did not receive private keys or credentials.

I understand that case 2 consumed its full gas limit and is not represented as
proof of a guarded-path revert. Historical point-in-time application-state raw
responses are unavailable because the state was pruned; the corresponding
invariant statement rests on contemporaneous execution narrative rather than
retained successful raw responses.

Durable signature or reviewed-commit reference: `REQUIRED`
